# taken from:
# https://github.com/selyunin/gtest_submodule
sudo: required
language: cpp
compiler: gcc

env:
  global:
    # Ubuntu version
    - LINUX_DIST=xenial
    - DEPS_DIR=${TRAVIS_BUILD_DIR}/deps
    # Global build options and C++ flags
    - CXX_FLAGS="-std=c++17 -Wall -pedantic -Werror -Wno-variadic-macros -Wno-long-long -Wno-shadow"
    # Misc
    - RUN_TESTS=true
    - CMAKE_OPTIONS="${CMAKE_OPTIONS} -DBUILD_TESTS:BOOL=ON ${ENV_CMAKE_OPTIONS}"

matrix:
  include:
    - os: linux
      sudo: true
      compiler: gcc
      addons:
        apt:
          packages: &precise_latest_boost_packages
            - gcc-8
            - g++-8
            # - clang-8
          sources: &precise_latest_boost_sources
            - ubuntu-toolchain-r-test

cache: apt
addons:
  apt:
    packages:
    - libtbb-dev

install:
  # - if [ "$CXX" = "clang++" ] && [ "$TRAVIS_OS_NAME" = "linux" ]; then export CXX="clang++-8" CC="clang-8"; fi
  - echo ${PATH}
  - echo ${CXX}
  - ${CXX} --version
  - ${CXX} -v

notifications:
  slack: theoptimisationgroup:tRbj8LtmY5KkuyL8qrQ7UOIQ

script:
  ############################################################################
  # Build main and tests
  ############################################################################
  - mkdir -p build
  - cd build
  - cmake ${CMAKE_OPTIONS} -DCMAKE_CXX_FLAGS=${CXX_FLAGS} ..
  - make
  - ctest

after_success:
  - echo "SUCCESS"